export interface ActivityHourFile {
  id: string;
  isCompleted: boolean;
  fileNamePrinciple?: string;
  fileUrl?: string; 
  createdAt: string;
  updatedAt: string;
  projectId: string;
  userId: string;
}